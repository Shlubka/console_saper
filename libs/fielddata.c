#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void GenCode(int diff, struct bombbas *bb, int rov, int col)
{
  srand(time(NULL));
  for(int i = 0; i < col; i++)
  {
    for(int i = 0; i < col; i++)
    {
      int r = rand() % 1;

    }
  }
}

